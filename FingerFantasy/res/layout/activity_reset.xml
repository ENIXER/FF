<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/resetLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.chokobo.fingerfantasy.ResetActivity" >

    <ImageButton
        android:id="@+id/noBtn"
        android:layout_width="90dp"
        android:layout_height="50dp"
        android:layout_alignRight="@+id/continue_imageview"
        android:layout_below="@+id/continue_imageview"
        android:background="@drawable/no"
        android:scaleType="fitCenter"
        />

    <ImageButton
        android:id="@+id/yesBtn"
        android:layout_width="90dp"
        android:layout_height="50dp"
        android:layout_alignLeft="@+id/continue_imageview"
        android:layout_alignTop="@+id/noBtn"
        android:background="@drawable/yes"
        android:scaleType="fitCenter" />

    <ImageView
        android:id="@+id/continue_imageview"
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="150dp"
        android:src="@drawable/cointinue" />

</RelativeLayout>
